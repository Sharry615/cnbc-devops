# Cloud Storage

Application-related buckets visible in the CNBC project include:

- `cnbc-production`
- `cnbc-staging`
- `cnbc-citizen-videostech-cloud`

The environment also contains Google-managed/function-related buckets. These are not treated as application architecture components unless specifically required.

Cloud Storage can serve static assets and other application-managed objects. Exact object lifecycle, retention and access policies are not established by the supplied screenshots.
